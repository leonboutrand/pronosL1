<ul>
  <% comments.each do |comment| %>
    <li id="comment-<%= comment.id %>" style="white-space: pre-line"><strong><%= comment.user.pseudo %></strong>: <%= comment.content %></li>
  <% end %>
</ul>
