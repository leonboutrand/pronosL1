<h1>Le live do Brasil</h1>

<%= render 'shared/select_matchday', path: '/live' %>

<div id="game-grid">
  <%= render 'shared/games_live', games: @games %>
</div>

<div id="live-ranking">
  <%= render 'shared/users_ranking', users: @users %>
</div>
