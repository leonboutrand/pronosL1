<h1>Pose tes pronos</h1>

<%= form_tag('/pronos', remote: true) do %>
  <%= label_tag(:matchday, 'Journée :') %>
  <%= select_tag(:matchday, options_for_select((1..38).to_a, Game.current_matchday), onchange: 'Rails.fire(this.form, "submit")') %>
<% end %>

<div id="bets-grid">
  <%= render 'shared/games_bets', games: @games %>
</div>
